// PASS: 4672 events seen (privileged logon sessions)
let lookback = 24h;
Event
| where TimeGenerated > ago(lookback)
| where EventLog == "Security"
| where EventID == 4672
| where Computer in~ ("win-ws1","win-ws2")
| summarize Count=count(), LastSeen=max(TimeGenerated) by Computer
